const baseURL = 'https://owu.linkpc.net/api/v3';

const urls = {
    cars: '/cars',
    auth: {
        login: '/auth',
        refresh: 'auth/refresh'
    },
    users: '/users'
};

export {
    baseURL,
    urls
};
